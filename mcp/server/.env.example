# Blockwork MCP Server Configuration
# Copy this file to .env and fill in your values

# ============================================
# Server Configuration
# ============================================

# Node environment (development, production, staging)
NODE_ENV=production

# Server port (default: 3000)
PORT=3000

# ============================================
# Authentication
# ============================================

# Comma-separated list of valid API keys
# Generate keys using: npm run mcp:generate-key
# Example: BLOCKWORK_API_KEY_abc123,BLOCKWORK_API_KEY_xyz789
API_KEYS=

# ============================================
# CORS Configuration
# ============================================

# Comma-separated list of allowed origins
# Leave empty in development to allow all origins
# Example: https://app.wakecap.com,https://staging.wakecap.com
ALLOWED_ORIGINS=

# ============================================
# Rate Limiting
# ============================================

# Rate limit window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# Max requests per window for unauthenticated users (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# Max requests per window for authenticated users (default: 500)
RATE_LIMIT_AUTHENTICATED_MAX=500

# ============================================
# Logging
# ============================================

# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# ============================================
# Security Notes
# ============================================

# 1. NEVER commit the .env file to version control
# 2. Generate strong API keys using: npm run mcp:generate-key
# 3. In production, always set NODE_ENV=production
# 4. Configure ALLOWED_ORIGINS to whitelist only trusted domains
# 5. Adjust rate limits based on your usage patterns
# 6. Keep LOG_LEVEL at 'info' or 'warn' in production
